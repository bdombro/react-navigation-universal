import { DefaultTheme } from 'react-native-paper';

export const ThemeConfig = {
    ...DefaultTheme,
    dark: false,
    // roundness: 2,
    colors: {
        ...DefaultTheme.colors,
        // primary: '#3498db',
        // accent: '#f1c40f',
    }
};
